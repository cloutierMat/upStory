route fetch  ==> 	routes/climb new endpoint get /api/climb/:crag/:route {climber:{name}}
										returns route description and climbers attempts and succes on specific route
									model/attempts => new function getOneByClimber
										returns route description and climbers attempts and succes on specific route
									public/climbs new fetch to return routes info and climbers attempts

clean up html to dry the code

prevent logging attempts if success was previously achieved

add message log to model/climbs routes

message log model/climbers.js getDetails Succesfully loaded climber from database undefined

load info in database